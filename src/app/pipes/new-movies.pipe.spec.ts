import { NewMoviesPipe } from './new-movies.pipe';

describe('NewMoviesPipe', () => {
  it('create an instance', () => {
    const pipe = new NewMoviesPipe();
    expect(pipe).toBeTruthy();
  });
});
